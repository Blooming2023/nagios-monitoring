
define command {
    command_name    check-host-alive
    command_line    /usr/lib/nagios/plugins/check_ping -H '$HOSTADDRESS$' -w 3000.0,80% -c 5000.0,100% -p 1
}

define command {
    command_name    notify-host-by-email
    command_line    /usr/bin/printf "%b" "Host Alert" | /usr/sbin/sendmail '$CONTACTEMAIL$'
}

define command {
    command_name    notify-service-by-email
    command_line    /usr/bin/printf "%b" "Service Alert" | /usr/sbin/sendmail '$CONTACTEMAIL$'
}
define command {
    command_name    check_nrpe
    command_line    /usr/lib/nagios/plugins/check_nrpe -H '$HOSTADDRESS$' -c '$ARG1$'
}

define command {
    command_name    check_ping
    command_line    /usr/lib/nagios/plugins/check_ping -H '$HOSTADDRESS$' -w '$ARG1$' -c '$ARG2$' -p 5
}

define command {
    command_name    check_local_disk
    command_line    /usr/lib/nagios/plugins/check_disk -w '$ARG1$' -c '$ARG2$' -p '$ARG3$'
}

define command {
    command_name    check_local_load
    command_line    /usr/lib/nagios/plugins/check_load -w '$ARG1$' -c '$ARG2$'
}

define command {
    command_name    check_local_procs
    command_line    /usr/lib/nagios/plugins/check_procs -w '$ARG1$' -c '$ARG2$' -s '$ARG3$'
}

define command {
    command_name    check_local_users
    command_line    /usr/lib/nagios/plugins/check_users -w '$ARG1$' -c '$ARG2$'
}
